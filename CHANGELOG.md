# CHANGELOG

本文件是 ProLab 的唯一发布公告来源。Web 端版本弹窗和文档站 `/docs/progress/changelog` 都读取这里。

- 准备中的变更先写到 `Unreleased`。
- 正式发版时，把 `Unreleased` 内容移动到新的 `vX.Y.Z - YYYY-MM-DD` 区块。
- 每次发版必须同步更新根目录 `VERSION`。

## Unreleased

- [新增] 爆款复刻升级为自动对象识别、P0 事件覆盖、单一母版和 1–1000 条完整候选的持久批量工作流，并增加分页结果库、质量门禁、费用授权与重复扣费保护。
- [修复] 画布文本、图片、视频、音频和爆款复刻成片改为 Gateway 持久化后台任务；刷新页面、切换画布或关闭浏览器不再中断已接受的任务，重新打开后按任务号恢复结果。
- [新增] 新增全局画布任务中心，可跨画布查看排队、生成、完成与失败状态，并支持跳转、取消和重试；前端不限制提交数量，实际并发由 Gateway 全局、能力和渠道配置控制。
- [优化] 每个画布改为独立 IndexedDB 记录和轻量索引，支持旧数据迁移、跨标签页失效通知与修订号保护，避免一个大画布保存或刷新覆盖其他画布。
- [修复] Gateway Worker 增加租约心跳、崩溃恢复、运行中取消和幂等请求；过期的 leased/submitting/running 任务会重新领取，已有上游视频任务号时只恢复轮询而不重复提交。
- [安全] 画布参考素材改为用户/画布范围内的私有持久化对象，任务行不保存 API Key 或 base64；生产 Gateway CORS 改为显式允许域名列表。
- [新增] 新增独立爆款复刻画布工作流，支持参考视频拉片、原创提示词策划及完整或分段视频生成。
- [新增] 新增 GouYingAi 项目与系统接入说明，明确 Gateway API、Canvas Agent、模型路由、浏览器本地数据和部署安全边界。
- [新增] 新增独立 AI 珠宝商品图工作流，支持从戒指原图批量生成首图、三视图、展示图、佩戴图和设计线稿。
- [调整] 应用白标为 GouYingAi，移除默认供应商、GitHub 和文档入口，并统一浏览器存储、WebDAV 与导出标识。
- [新增] 画布节点支持统一维护名称字段，默认显示在节点上方，并可直接双击名称编辑。
- [新增] 画布新增组节点，支持节点拖入/拖出分组、拖拽高亮吸附和移动组时带动子节点。
- [调整] 画布节点顶部工具条改为点击选中节点后显示，避免鼠标经过节点时频繁弹出。
- [修复] 在线画布助手新增视频理解输入，OpenAI 兼容的 Chat 模型可通过 `video_url` 分析选中的本地或公网视频。
- [新增] 视频节点工具栏支持“反推提示词”，会按视频动作、镜头运动和节奏生成对应的分析提示词流程。
- [优化] 视频反推提示词改为按上传视频真实切镜头输出分镜，覆盖完整时间轴并提高视频理解的输出上限。
- [修复] `grok-imagine-video` 改为兼容 JSON `/v1/videos`、`images` 多图数组和 `task_id` 轮询，可直接使用本地参考图 Data URI。
- [修复] 模型请求按渠道、模型和声明能力严格匹配；失效渠道、同名歧义或能力不符时直接拒绝，不再自动重绑或回退到其他模型。
- [调整] AI 渠道、API Key、模型发布和排序统一由管理后台维护，前台移除重复的渠道、模型与一键接入入口。

## v0.6.0 - 2026-07-09

- [新增] 新增Codex App插件支持。
- [新增] 配置与用户偏好新增独立页面和 Codex 连接配置 Tab。
- [新增] 新增GitHub Pages 前端静态站点发布 workflow。
- [新增] 图片切图支持等分线直接拖拽调整，并可新增、删除和重置横向 / 纵向切图线。
- [调整] Docker 运行镜像改为 nginx 静态托管。
- [调整] 移除网站Agent模式，专注于连接Codex Agent操作画布
- [修复] 修复生图工作台重试成功结果刷新后丢失的问题。
- [修复] 修复 Gemini 调用格式生图未传递尺寸比例配置的问题。
- [修复] 修复前端 TypeScript 构建报错。
- [修复] 修复画布生成配置切换文本/视频/音频模式时模型仍显示为生图模型的问题。
- [修复] 兼容中转站视频任务直接返回视频 URL 且没有 `/content` 接口的情况，并优化失败原因展示。

## v0.5.0 - 2026-07-05

- [新增] 渠道兼容Gemini格式。
- [调整] 前端从 Next.js 迁移到 Vite，项目改为静态前端构建。
- [调整] 移除已 404 的 EvoLinkAI 提示词来源。

## v0.0.2 - 2026-06-21

- [优化] 同名模型选择体验。不同渠道或不同 API Key 下的同名模型会显示渠道序号、Base URL 和 Key 后四位，并在画布、Agent、生图工作台和视频工作台中精确路由到对应 API Key。
- [修复] 旧数据里的裸模型名或已失效渠道模型会重新匹配当前可用渠道，避免同名模型静默调用到错误 Key。
- [修复] 画布节点提示面板底部图像设置按钮与生成/停止按钮重叠的问题，模型名和设置文案过长时会自动截断。
- [优化] 从已有节点拉出同类型新节点时，自动复用源节点已选择的模型，无需重新选择。

## v0.0.1 - 2026-06-20

### 版本定位

ProLab v0.0.1 是基于 `basketikun/infinite-canvas` 的初始迁移版本。这个版本保留原项目 AGPL-3.0 授权声明和 `canvas.best` 来源标识，并把 ProAPI 用户常用能力整合到 Pro Canvas 工作台。

### 面向用户的变化

- [新增] 无需注册登录。应用默认在浏览器中运行，用户自行配置 ProAPI 或 OpenAI 兼容接口即可使用。
- [新增] ProAPI 一键接入。支持默认上游、URL 参数预填、API Key 粘贴、模型拉取、模型选择和本地持久化。
- [新增] 模型图标和能力识别。模型列表会自动识别 GPT、Claude、Gemini、Grok、Doubao、Hunyuan 等模型，并显示对应图标。
- [新增] 标准模型参数适配。覆盖 OpenAI Images、image edits、OpenAI Video 和 Grok video chat 兼容协议，`hunyuan-image-v3` 等图像模型不会错误走 chat 协议。
- [新增] 本地数据持久化。API 配置、画布、素材、生图记录和视频记录默认保存在当前浏览器本地。

### 数据保存说明

- 浏览器本地数据会保留到用户清理站点数据、切换浏览器、切换域名、使用无痕窗口或浏览器触发存储清理为止。
- 这不是云端账号存储，也不能保证跨设备永久保留。
- 长期保存建议使用画布/素材导出，或在设置中配置 WebDAV 同步。WebDAV 同步用于画布、素材和生成记录，不同步 API Key。

### 当前范围

- 本版本只接入海外标准模型参数协议，不接入 `mj-proxy`、`koukoutu` 等非本期目标协议。
